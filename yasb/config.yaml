# Ahmadinne's Yasb Bar
watch_stylesheet: true
watch_config: true
debug: false
update_check: true # Enable automatic update check.
show_systray: true # Show YASB on the system tray.
bars:
  primary-bar:
    enabled: true
    screens: ["*"]
    class_name: "yasb-bar"
    alignment:
      position: "bottom"
      center: true
    animation:
      enabled: false
    blur_effect:
      enabled: false
    window_flags:
      always_on_top: false
      windows_app_bar: true
    dimensions:
      width: "100%"
      height: 30
    padding:
      top: 0
      left: 0
      bottom: 0
      right: 0
    layouts:
      left:
        alignment: "left"
        stretch: true
      center:
        alignment: "center"
        stretch: true
      right:
        alignment: "right"
        stretch: true
    widgets:
      left: [ "glazewm_workspaces" ]
      center: [ ]
      right: [ "battery", "wifi", "volume", "clock" ]
widgets:
  glazewm_workspaces:
    type: "glazewm.workspaces.GlazewmWorkspacesWidget"
    options:
      offline_label: "\u23fc Glazewm Offline"
      hide_empty_workspaces: true
      hide_if_offline: false
      animation: false
      app_icons:
        enabled_populated: false
        enabled_active: false
        size: 20
        hide_label: true
        hide_duplicates: true
        hide_floating: false
  wifi:
    type: "yasb.wifi.WifiWidget"
    options:
      label: "<span>{wifi_icon}</span> {wifi_name}"
      callbacks: 
        on_left: "toggle_menu"
        on_middle: "exec cmd.exe /c start ms-settings:network"
      wifi_icons: [
        "<img src='$env:USERPROFILE\\.config\\yasb\\svg\\wifi-off.svg' />",  # Icon for 0% strength
        "<img src='$env:USERPROFILE\\.config\\yasb\\svg\\wifi-low.svg' />",  # Icon for 1-24% strength
        "<img src='$env:USERPROFILE\\.config\\yasb\\svg\\wifi-low.svg' />",  # Icon for 25-49% strength
        "<img src='$env:USERPROFILE\\.config\\yasb\\svg\\wifi-high.svg' />",  # Icon for 50-74% strength
        "<img src='$env:USERPROFILE\\.config\\yasb\\svg\\wifi.svg' />"   # Icon for 75-100% strength
      ]
      menu_config:
        blur: false
        round_corners: false
        alignment: "center"
        direction: "up"
        offset_top: 6
        offset_left: 0
        wifi_icons_secured: [
          "\ue670",
          "\ue671",
          "\ue672",
          "\ue673",
        ]
        wifi_icons_unsecured: [
          "\uec3c",
          "\uec3d",
          "\uec3e",
          "\uec3f",
        ]
  volume:
    type: "yasb.volume.VolumeWidget"
    options:
      label: "<span>{icon}</span> {level}"
      volume_icons:
        - "<img src='$env:USERPROFILE\\.config\\yasb\\svg\\volume-x.svg' />"  # Icon for muted
        - "<img src='$env:USERPROFILE\\.config\\yasb\\svg\\volume-1.svg' />"  # Icon for 0-10% volume
        - "<img src='$env:USERPROFILE\\.config\\yasb\\svg\\volume-1.svg' />"  # Icon for 11-30% volume
        - "<img src='$env:USERPROFILE\\.config\\yasb\\svg\\volume-1.svg' />"  # Icon for 31-60% volume
        - "<img src='$env:USERPROFILE\\.config\\yasb\\svg\\volume-2.svg' />"  # Icon for 61-100% volume
      audio_menu:
        alignment: "center"
        direction: "up"
        blur: false
        round_corners: false
      callbacks:
        on_left: "toggle_volume_menu"
        on_right: "toggle_mute"
  clock:
    type: "yasb.clock.ClockWidget"
    options:
      # label: "<span><img src='$env:USERPROFILE\\.config\\yasb\\svg\\clock.svg'/></span>{%H:%M - %a, %b %d}"
      label: "<span><img src='$env:USERPROFILE\\.config\\yasb\\svg\\clock.svg'/></span>{%H:%M}"
  battery:
    type: "yasb.battery.BatteryWidget"
    options:
      label: "<span>{icon}</span> {percent}%"
      status_thresholds:
        critical: 20
        low: 40
        medium: 60
        high: 80
        full: 100
      charging_options:
        icon_format: "{charging_icon}"
      status_icons:
        icon_charging: "<img src='$env:USERPROFILE\\.config\\yasb\\svg\\battery-charging' />"
        icon_critical: "<img src='$env:USERPROFILE\\.config\\yasb\\svg\\battery-warning' />"
        icon_low: "<img src='$env:USERPROFILE\\.config\\yasb\\svg\\battery-low' />"
        icon_high: "<img src='$env:USERPROFILE\\.config\\yasb\\svg\\battery-medium' />" 
        icon_full: "<img src='$env:USERPROFILE\\.config\\yasb\\svg\\battery-full' />" 
